# プロジェクトルール

## 作業開始時

ブランチ名を確認し以下を判断すること：

**Issue番号がある場合**（例: feature/issue-42-xxx）
1. `gh issue view {番号}` で内容を取得する
2. 受け入れ条件を確認してから実装を始める
3. PR本文に `Closes #番号` を含める

**Issue番号がない場合**
1. 作業内容をユーザーに確認してから進める
2. 実装後のPRタイトルは変更内容を簡潔に書く

## 完了条件
- 実装後は以下の順序で必ず実施すること：
  1. 静的コードチェック（編集ごとにIDEの診断結果を確認し、自分の変更行に警告がないことを確認する）
  2. テスト実行（`python tests/test_diff.py`）
  3. ドキュメント更新（README.md, SPEC.md 等）
  4. コミット実施の許可をユーザーに求める
- テストが通らない場合は報告してから次に進むこと
- 静的チェックの警告は「既存だから無視」せず、自分の変更が原因でないことを確認すること

## 成果物ルール
- 出力前に行数・カラム数をカウントして報告すること

## 作業ルール
- 参照系の作業は許諾なしに実行して良い
- 更新系の作業は禁止事項を遵守すること

## 禁止事項
- 確認なしにファイルを上書きしないこと
- 書き込み操作の前は必ず確認を求めること