# excel-diff ドメインコンテキスト

2つのExcelブック（またはシート）を比較し、行・セル単位の差分をHTMLレポートとして出力するツール。単一コンテキスト。

## Language

### 差分の基本語彙

**行ステータス**:
1行の比較結果を表す4分類（EQUAL / DELETE / INSERT / MODIFY）。
_Avoid_: 差分タイプ、行タイプ

**LCSモード**:
旧シート・新シートの行を`SequenceMatcher`で順序ベースに対応付ける差分モード（`diff_mode="lcs"`、デフォルト）。キーの概念を持たない。

**キーJOINモード**:
指定した列（主キー）の値が一致する行同士を対応付ける、順序に依存しない差分モード（`diff_mode="key"`）。

**主キー**:
キーJOINモードで行の対応付けに使う列（複数列指定可、順序保持）。値が完全一致した行同士のみを結合する。値そのものは差分比較対象に含まれない（一致が前提のため）。
_Avoid_: キー列、識別列

### サブキー照合

主キーで一意に対応付けられなかった行を、もう一段階の照合で救済する仕組み。

**サブキー**:
主キーでの対応付けに失敗した行に対してのみ試みる、2段目の照合キー。キーJOINモード限定。主キー列と重複する列は指定できない（設定エラー）。
_Avoid_: 副キー、フォールバックキー

**未マッチプール**:
主キーで対応が確定しなかった行の集まり。以下をすべて含む：キー値が空欄の行、キー値が重複していた行の2件目以降、そして有効な一意キーを持つが相手側にそのキー値の行が存在しない行（＝本来ならそのままDELETE/INSERTになるはずだった行）。サブキー照合の対象であり、そこでも対応が付かなかった行は元の由来ごとに異なる行き先へ進む：空欄/重複キー由来の行は既存のLCSフォールバックへ、有効キーだが相手不在だった行はそのままDELETE/INSERTへ（LCSは経由しない）。

**曖昧一致**:
未マッチプール内で、あるサブキー値を持つ行が旧シート側・新シート側のどちらかで2件以上（またはどちらかが0件）存在し、1対1に確定できない状態。曖昧一致と判定された行は対応付けを行わず、そのまま未マッチプールに残す（安全側に倒す）。

**サブキー照合ペア**:
サブキーによって対応付けられた旧行・新行の組。主キー一致による対応付けと異なり、旧行・新行の主キー列の値そのものが食い違っている（＝主キーでは一致しなかった）ため、その回の比較に限り主キー列も通常の差分比較対象列として扱う。HTML出力では、この主キー列のセルを通常のMODIFY（黄色）とは別の背景色で示す。
